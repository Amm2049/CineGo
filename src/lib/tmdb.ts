// src/lib/tmdb.ts
// TMDB REST API client wrapper
// TODO: Phase 8 — Implement TMDB search and movie detail fetch for admin movie import

// searchMovies(query: string) — calls GET /search/movie
// getMovieDetails(tmdbId: number) — calls GET /movie/{id}

export {};
